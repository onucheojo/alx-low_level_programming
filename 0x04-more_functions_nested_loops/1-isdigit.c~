#include "main.h"
#include <stdio.h>

/**
 * _isdigit - check if parameter is a digit (0-9)
 *
 * Return: 1 if trur, 0 otherwise.
 **/

int _isdigit()
{
  if(c >= 48 && c <= 57)
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
  
