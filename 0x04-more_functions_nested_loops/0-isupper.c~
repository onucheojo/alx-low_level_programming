#include "main.h"

/**
 * _isupper - labels a character
 * if uppercase returns 1
 * if lowercase returns 0
**/

int _isupper(int c)
{
  if( c >= 68 && c <= 90 )
    {
      return (1);
    }
  else
    {
    return (0);
    }
}
