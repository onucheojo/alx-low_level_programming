#include "main.h"
#include <stdio.h>

/**
 * print_most_numbers - prints numbers for 1-9
 * except for numbers 2 & 4
 *
 * return: void
 **/

void print_most_numbers(void)
{
  char c;

  c = 48;
  while(c <= 57)
    {
      putchar(c);
      ++c;
      if(c == 50 || c == 52)
	{
	  ++c;
	}
    }
}
