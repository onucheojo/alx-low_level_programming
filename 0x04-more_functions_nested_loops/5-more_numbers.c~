#include "main.h"
#include <stdio.h>

/**
 * most_numbers - prints numbers from 0-14
 * consecutively for 10x
 *return: void
 **/
void more_numbers(void)
{
  int n;
  int m;
  m = 0;
  while(m <= 10)
    {
      n = 0;
      while(n <= 14)
	{
	  _putchar(n);
	  ++n;
	}
      _putchar('\n');
      ++m;
    }
}
